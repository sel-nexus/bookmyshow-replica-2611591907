import type Database from 'better-sqlite3';
export interface Confirmation { confirmationId:string; movie:{id:number;title:string}; theatre:{id:number;name:string}; seats:string[]; }
/** Persist and project booking records from SQLite. */
export class BookingRepository { constructor(private readonly db:Database.Database) {} isOffered(movieId:number,theatreId:number){return Boolean(this.db.prepare('SELECT 1 FROM movie_theatres WHERE movie_id=? AND theatre_id=?').get(movieId,theatreId));} create(userId:number,input:{movieId:number;theatreId:number;seats:string[];paymentMethod:'CARD'|'UPI';totalPrice:number}):Confirmation{return this.db.transaction(()=>{const result=this.db.prepare('INSERT INTO bookings (user_id,movie_id,theatre_id,seats,payment_method,total_price) VALUES (?,?,?,?,?,?)').run(userId,input.movieId,input.theatreId,JSON.stringify(input.seats),input.paymentMethod,input.totalPrice);const row=this.db.prepare('SELECT b.id,m.id movieId,m.title,t.id theatreId,t.name,b.seats FROM bookings b JOIN movies m ON m.id=b.movie_id JOIN theatres t ON t.id=b.theatre_id WHERE b.id=?').get(result.lastInsertRowid) as {id:number;movieId:number;title:string;theatreId:number;name:string;seats:string};return {confirmationId:`BMS-${row.id}`,movie:{id:row.movieId,title:row.title},theatre:{id:row.theatreId,name:row.name},seats:JSON.parse(row.seats) as string[]};})(); }}