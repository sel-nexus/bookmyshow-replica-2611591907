import { test, expect } from '@playwright/test';
test('shows unselected grid before applying prescribed seats',async({page})=>{await page.goto('/'); await page.getByRole('link',{name:'Start booking'}).click(); await page.getByLabel('Mobile number').fill('9876543210'); await page.getByRole('button',{name:'Send OTP'}).click(); await page.getByLabel('One-time password').fill('1234'); await page.getByRole('button',{name:'Verify and continue'}).click(); await page.getByRole('button',{name:'Paradise'}).click(); await page.getByRole('button',{name:'Sandhya 70mm'}).click(); await expect(page.getByText('No seats selected yet.')).toBeVisible(); await expect(page.getByLabel('A1 unselected')).toBeVisible(); await page.getByRole('button',{name:'Select Seats'}).click(); await expect(page.getByRole('heading',{name:'Payment loading soon'})).toBeVisible();});