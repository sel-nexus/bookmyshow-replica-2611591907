import { test, expect } from '@playwright/test';
test('shows method-specific payment inputs and processing state',async({page})=>{await page.goto('/');await page.getByRole('link',{name:'Start booking'}).click();await page.getByLabel('Mobile number').fill('9876543210');await page.getByRole('button',{name:'Send OTP'}).click();await page.getByLabel('One-time password').fill('1234');await page.getByRole('button',{name:'Verify and continue'}).click();await page.getByRole('button',{name:'Paradise'}).click();await page.getByRole('button',{name:'Sandhya 70mm'}).click();await page.getByRole('button',{name:'Select Seats'}).click();await expect(page.getByLabel('Card Number')).toBeVisible();await page.getByLabel('UPI').click();await expect(page.getByLabel('UPI ID')).toBeVisible();await page.getByRole('button',{name:'Pay Rs. 450'}).click();await expect(page.getByText('Processing Payment...')).toBeVisible();});